import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: `'Mulish', sans-serif`,
    button: {
      textTransform: "none",
    },
  },
  palette: {
    background: {
      default: "#F6F8F9",
    },
  },
});